server:
  host: "0.0.0.0"
  port: 8080

claude:
  command: "claude"
  timeout: 300

git:
  auto_commit: true
  commit_prefix: "claude"

app:
  default_mode: "interactive"
  max_attachment_size: 5242880
  log_retention: "forever"
